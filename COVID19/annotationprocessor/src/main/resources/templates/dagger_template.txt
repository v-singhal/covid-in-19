package com.vbstudio.covid19.injector.modules;

import dagger.Module;
import dagger.android.ContributesAndroidInjector;
import com.vbstudio.covid19.core.injection.exposed.ActivityScope;
import com.vbstudio.covid19.core.injection.exposed.FragmentScope;

{{#imports}}
import {{.}};
{{/imports}}

@Module
public abstract class {{moduleName}} {

     {{#builders}}
     @ContributesAndroidInjector(modules={
          {{#modules}}
          {{.}}.class,
          {{/modules}}
     })
     @{{scope}}
     abstract {{name}} bind{{name}}();

     {{/builders}}
}